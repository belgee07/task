{% extends 'base.html.twig' %}

{% block title %}Edit Department{% endblock %}

{% block body %}
    <h1>Edit Department</h1>

    {{ form_start(form) }}
        <div class="mb-3">
            {{ form_row(form.departmentName, {'attr': {'class': 'form-control'}}) }}
        </div>
        <button type="submit" class="btn btn-warning">Update</button>
    {{ form_end(form) }}
{% endblock %}
